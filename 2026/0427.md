### 파일 암호화
- AppApiCTL "/api/app/uploadVoiceFile.do" 
- FileCTL "/api/file/uploadFile.do" 
- 음성파일 확장자만 isEncrypt = true로 타게 생성 (writeEncryptedFile)

인/아웃바운드 파일 -> 모듈 통해서 저장
앱 사용신청 파일 -> 서버단에서 저장

### 파일 복호화
- FileCTL "/api/file/downloadFile/**/{path}/{fileName:.+}"  (웹/앱 동일)
- 음성파일 확장자만 isEncrypt = true (decryptAndStream)


### 테스트
- 앱 주민 사용신청 음성 파일 암/복호화 
- 인/아웃바운드 파일 암/복호화
    ```
    2026-04-28 16:30:54,434] ERROR: [make_tts] 요청 실패: HTTPConnectionPool(host='host.docker.internal', port=18081): Read timed out. (read timeout=10)

[2026-04-28 16:30:54,434] ERROR: [tts_play] TTS 생성 실패
    ```

---

### 상위 주소록도 보여지게 수정
- tb_address_group (주소록 목록 참고) tb_user_group